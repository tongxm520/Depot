$('#notice').hide();
$('#cart').html("<%= escape_javascript(render(@cart)) %>");
<%if @cart.total_items == 1%>
$("#highlight").click(function() {
  $("#cart").toggle( "blind" );
});
<%end%>
$("#highlight").click(function() {
  $("#current_item").toggle("highlight");
});

